# Bubblicious


```bash
npm install
cp .env.example .env    # add MySQL config etc to .env
composer install
php artisan key:generate
php artisan migrate
```

Running locally:
```bash
php artisan serve
```
